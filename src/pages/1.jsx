import axios from "axios"

const getCoins = async () => {
    const respone = await axios.get(
        "url",
        {
            headers :
            {
                
            }
        }
    )
}